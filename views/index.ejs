<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <p>Welcome to the message board.</p>
    <a href="/new">Add a new message</a>
    <ul>
        <% messages.forEach(msg => { %>
            <li><strong><%= msg.user %></strong>: <%= msg.text %> <em>(<%= msg.added.toLocaleString() %>)</em></li>
        <% }) %>
    </ul>
</body>
</html>