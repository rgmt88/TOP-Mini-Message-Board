<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <p>Welcome to the message board. Feel free to share your thoughts!</p>

    <section>
        <h3>💡 Guidelines</h3>
        <ul>
            <li>Be respectful - no offensive or abusive language.</li>
            <li>Keeps messages clear and concise.</li>
            <li>Don't share personal information.</li>
        </ul>
    </section>

    <a href="/new">➕ Add a new message</a>
    
    <ul>
        <% messages.forEach(msg => { %>
            <li><em>(<%= msg.added.toLocaleString() %>)</em> <strong><%= msg.user %></strong>: <%= msg.text %></li>
        <% }) %>
    </ul>
</body>
</html>